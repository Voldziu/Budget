class Category {
    constructor(id, name, icon, color, budget) {
      this.id = id;
      this.name = name;
      this.icon = icon;
      this.color = color;
      this.budget = budget; // Monthly budget limit for this category
    }
  }